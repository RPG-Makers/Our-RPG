using UnityEngine;

public class VillageHeadman : QuestGiver
{
    //protected override void GiveQuest(GameObject player)
    //{
        //if (givedQuest) { return; }

        //Task[] tasks = new Task[1] { new GoTask("Leave the village!", "Leave the village and help villager on your way", _placeManager.GetComponent<PlacesLinks>().ExitVillage) };
        //_quest = new Quest("Leave the village!", "Talk", tasks);
        //base.GiveQuest(player);
    //}
}
